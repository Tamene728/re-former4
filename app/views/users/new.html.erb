     <!--  <form action="/new" accept-charset="UTF-8" methode="POST" data-turbo=false>
            <label for="email">Email</label>
            <input type="text" id="email" name="user[email]"><br><br>

            <label for="uname">Username</label>
            <input type="text" id="uname" name="user[username]"><br><br>

            <label for="pw">Password</label>
            <input type="password" id="pw" name="user[password]"><br><br>

            <input type="hidden" name="authenticity_token"
                    value="<%= form_authenticity_token %>"
        >

            <input type="submit" value="Submit">




        </form>
        -->


       <!-- <%= form_tag('/new') do %>
       <div>
         <%= label_tag(:email, 'Email')%>
         <%= text_field_tag(:email)%>
       </div>
         <br>
        <div>
         <%= label_tag(:username, 'Username')%>
         <%= text_field_tag(:username)%>
        </div>
        <br>
        <div>
         <%= label_tag(:password, 'Password')%>
         <%= password_field_tag(:password)%>
         </div>
        <br>
         <%= submit_tag('Submit')%>

       <% end %>
       -->

  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


       <%= form_with model: @user do |form|%>
        <div>
          <%= form.label :email, 'Email'%>
          <%= form.email_field :email%>
        </div>
        <br>
        <div>
          <%= form.label :username, 'Username'%>
          <%= form.text_field :username%>
        </div>
        <br>
        <div>
          <%= form.label :password, 'Password'%>
          <%= form.password_field :password%>
        </div>
        <br> 
        <%= form.submit 'Submit'%>
       <% end %>